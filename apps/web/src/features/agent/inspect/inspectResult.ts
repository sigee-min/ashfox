// Overview includes exact entry/build identities and bounded review progress.
// Keep the same finite ceiling as detailed inspection so valid longer package
// names do not make the only source of current guards unreadable.
export const DEFAULT_INSPECT_LIMIT = 16_384;
export const DETAIL_INSPECT_LIMIT = 16_384;
